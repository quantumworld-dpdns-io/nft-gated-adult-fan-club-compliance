import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";
import { Navbar } from "@/components/Navbar";
import { Footer } from "@/components/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "FanClub — NFT-Gated Adult Content Platform",
  description:
    "A compliance-first platform for NFT-gated adult fan clubs with zero-knowledge age verification, secure membership management, and regulatory compliance built in.",
  keywords: ["NFT", "fan club", "age verification", "zero-knowledge", "blockchain", "compliance"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        <Providers>
          <div className="min-h-screen flex flex-col">
            <Navbar />
            <main className="flex-1">{children}</main>
            <Footer />
          </div>
        </Providers>
      </body>
    </html>
  );
}
